import React, { useEffect, useMemo, useState } from 'react';
import { createRoot } from 'react-dom/client';
import { Activity, ArrowLeft, BriefcaseBusiness, Check, ChevronRight, CircleHelp, FileText, FolderOpen, LockKeyhole, Plus, RefreshCw, Search, Send, Settings as SettingsIcon, Sparkles, Trash2, Upload, UserRound, WandSparkles, X } from 'lucide-react';
import type { ExtensionMessage, ExtensionResponse } from '../types/messages';
import type { FormField, Job, JobSession, KnowledgeItem, Resume, Settings, UserProfile } from '../types/models';
import { repos } from '../repositories/repositories';
import { defaultProfile, defaultSettings } from '../services/profile/defaults';
import { extractResumeText, heuristicResumeData } from '../services/resume/parser';
import { getLLMProvider } from '../services/llm/providerFactory';
import { normalizePageContext } from '../services/parser/normalization';
import { findDuplicateJob } from '../services/parser/deduplication';
import { buildFieldContext } from '../services/context/buildFieldContext';
import { fetchOpenRouterModels, type OpenRouterModel } from '../services/llm/models';
import { now, uid } from '../utils/id';
import { syncRecord } from '../services/api/backendClient';
import { downloadResumeFile, loginBackend, pullRecords, uploadResumeFile } from '../services/api/backendClient';
import './styles.css';

type View = 'dashboard' | 'resumes' | 'session' | 'settings' | 'knowledge';
const statusColors: Record<string, string> = { Saved: 'blue', Analyzing: 'purple', Applied: 'green', Interview: 'orange', Rejected: 'red', Offer: 'green', Withdrawn: 'gray', Archived: 'gray' };
const profileValue = (label: string, profile: UserProfile) => { const l = label.toLowerCase(); if (/first.?name/.test(l)) return profile.firstName; if (/last.?name/.test(l)) return profile.lastName; if (/full.?name|name/.test(l)) return profile.fullName; if (/e-?mail/.test(l)) return profile.email; if (/phone|mobile/.test(l)) return profile.phone; if (/address/.test(l)) return profile.address; if (/city/.test(l)) return profile.city; if (/province|state/.test(l)) return profile.province; if (/postal|zip/.test(l)) return profile.postalCode; if (/linkedin/.test(l)) return profile.linkedin; if (/portfolio/.test(l)) return profile.portfolio; if (/website/.test(l)) return profile.website; if (/authorization|sponsor/.test(l)) return profile.workAuthorization; return ''; };
const activeTab = async () => (await chrome.tabs.query({ active: true, currentWindow: true }))[0];
async function pageMessage(message: ExtensionMessage): Promise<ExtensionResponse> { const tab = await activeTab(); if (!tab.id) throw new Error('No active tab.'); return await chrome.runtime.sendMessage({ ...message, tabId: tab.id }) as ExtensionResponse; }

function App() {
  const [view, setView] = useState<View>('dashboard'); const [jobs, setJobs] = useState<Job[]>([]); const [resumes, setResumes] = useState<Resume[]>([]); const [profile, setProfile] = useState<UserProfile>(defaultProfile); const [settings, setSettings] = useState<Settings>(defaultSettings); const [knowledge, setKnowledge] = useState<KnowledgeItem[]>([]); const [session, setSession] = useState<JobSession | null>(null); const [selectedJobId, setSelectedJobId] = useState<string>(); const [fields, setFields] = useState<FormField[]>([]); const [busy, setBusy] = useState(''); const [toast, setToast] = useState(''); const [query, setQuery] = useState('');
  const refresh = async () => { const savedSettings = await repos.settings.get(); const mergedSettings = savedSettings ? { ...defaultSettings, ...savedSettings } : defaultSettings; if (mergedSettings.syncEnabled && mergedSettings.backendUrl && mergedSettings.backendToken) { try { const remote = await pullRecords(mergedSettings); for (const record of remote) { if (record.entity_type === 'job') await repos.jobs.put(record.payload as unknown as Job); else if (record.entity_type === 'resume') { const existing = await repos.resumes.get(record.entity_id); let file = existing?.file ?? new Blob(); if (!existing?.file) { try { file = await downloadResumeFile(mergedSettings, record.entity_id); } catch { /* metadata remains usable if the original file is unavailable */ } } await repos.resumes.put({ ...(record.payload as unknown as Resume), file, id: record.entity_id }); } else if (record.entity_type === 'session') await repos.sessions.put(record.payload as unknown as JobSession); else if (record.entity_type === 'knowledge') await repos.knowledge.put(record.payload as unknown as KnowledgeItem); else if (record.entity_type === 'profile') await repos.profile.put(record.payload as unknown as UserProfile); else if (record.entity_type === 'application') await repos.applications.put(record.payload as never); } } catch { setToast('Server unavailable; showing local cache'); } } const [j, r, p, s, k] = await Promise.all([repos.jobs.list(), repos.resumes.list(), repos.profile.get(), repos.settings.get(), repos.knowledge.list()]); setJobs(j.sort((a, b) => b.lastActivityAt.localeCompare(a.lastActivityAt))); setResumes(r); setProfile(p ?? defaultProfile); setSettings(s ? { ...defaultSettings, ...s } : defaultSettings); setKnowledge(k); };
  const sync = (type: string, id: string, payload: unknown) => { syncRecord(settings, type, id, payload).catch(() => setToast('Saved locally; server sync will retry when configured')); };
  useEffect(() => { refresh().catch((e) => setToast(e instanceof Error ? e.message : 'Unable to load local data')); }, []);
  useEffect(() => { if (!toast) return; const timer = setTimeout(() => setToast(''), 3500); return () => clearTimeout(timer); }, [toast]);
  const notifyError = (error: unknown) => setToast(error instanceof Error ? error.message : 'Something went wrong');
  const analyze = async () => { setBusy('analyze'); try { const response = await pageMessage({ type: 'CAPTURE_PAGE_CONTEXT' }); if (!response.ok || !('context' in response)) throw new Error(!response.ok ? response.error : 'Page context unavailable'); const responseContext = response.context; const normalized = normalizePageContext(responseContext); const existing = findDuplicateJob(normalized, jobs); const stamp = now(); const job = existing ? { ...existing, ...normalized, id: existing.id, discoveredAt: existing.discoveredAt, status: 'Analyzing' as const, lastSeenAt: stamp } : { ...normalized, status: 'Analyzing' as const }; await repos.jobs.put(job); const started = stamp; const nextSession: JobSession = { id: uid('session'), tabId: (await activeTab()).id, jobId: job.id, url: job.sourceUrl, startedAt: started, updatedAt: started, pageContext: responseContext, events: [{ id: uid('event'), type: 'context_captured', timestamp: started }], generatedAnswers: [], status: 'active' }; await repos.sessions.put(nextSession); setSession(nextSession); const provider = getLLMProvider(settings); const result = await provider.analyzeJob(job, resumes, profile); const best = result.matches[0]; const updated = { ...job, status: 'Saved' as const, analysis: result.analysis, matches: result.matches, matchScore: best?.score, resumeId: best?.resumeId, lastActivityAt: now() }; await repos.jobs.put(updated); setJobs((items) => [updated, ...items.filter((item) => item.id !== job.id)]); setSession({ ...nextSession, updatedAt: now() }); setView('session'); setToast(`${existing ? 'Updated existing job' : 'New job'} analyzed with ${provider.name}`); } catch (error) { notifyError(error); } finally { setBusy(''); } };
  const uploadResume = async (file: File) => {
    setBusy('upload');
    try {
      const extension = file.name.split('.').pop()?.toLowerCase();
      if (extension !== 'pdf' && extension !== 'docx') throw new Error('Please select a PDF or DOCX resume.');

      let text = '';
      let parseWarning = '';
      try {
        text = await extractResumeText(file);
        if (!text.trim()) parseWarning = 'Text was not found; the original file was saved for review.';
      } catch {
        parseWarning = 'Text extraction failed; the original file was saved. Try a text-based PDF or DOCX.';
      }

      const provider = getLLMProvider(settings);
      let data = heuristicResumeData(text);
      if (text.trim()) {
        try { data = await provider.extractResumeData(text); } catch { data = heuristicResumeData(text); }
      }
      const stamp = now();
      const resume: Resume = { id: uid('resume'), name: file.name.replace(/\.(pdf|docx?)$/i, ''), fileName: file.name, mimeType: file.type, file, parsedText: text, structuredData: data, targetRoles: data.jobTitles, preferred: resumes.length === 0, version: 1, createdAt: stamp, updatedAt: stamp };
      await repos.resumes.put(resume);
      sync('resume', resume.id, { ...resume, file: undefined });
      uploadResumeFile(settings, resume.id, file, file.name).catch(() => setToast('Resume saved locally; server file upload will retry later'));
      const merged = { ...profile, firstName: profile.firstName || data.firstName || data.fullName?.split(' ')[0] || '', lastName: profile.lastName || data.lastName || '', fullName: profile.fullName || data.fullName || '', email: profile.email || data.email || '', phone: profile.phone || data.phone || '', skills: [...new Set([...profile.skills, ...data.skills])] };
      await repos.profile.put(merged);
      sync('profile', 'default', merged);
      setProfile(merged);
      setResumes((items) => [...items, resume]);
      setToast(parseWarning || `Imported ${file.name} with ${provider.name}`);
    } catch (error) {
      notifyError(error);
    } finally {
      setBusy('');
    }
  };
  const detectForm = async () => { setBusy('detect'); try { const response = await pageMessage({ type: 'DETECT_FORM_FIELDS' }); if (!response.ok || !('fields' in response)) throw new Error(!response.ok ? response.error : 'Form fields unavailable'); setFields(response.fields); setView('session'); setToast(`${response.fields.length} application fields detected`); } catch (error) { notifyError(error); } finally { setBusy(''); } };
  const fillForm = async () => { setBusy('fill'); try { const values = fields.filter((field) => field.category !== 'ignore').map((field) => ({ selector: field.selector, value: field.value || profileValue(field.label, profile) })).filter((field) => field.value); const response = await pageMessage({ type: 'FILL_FORM_FIELDS', fields: values }); if (!response.ok || !('filled' in response)) throw new Error(!response.ok ? response.error : 'Form fill failed'); setFields((items) => items.map((field) => values.some((value) => value.selector === field.selector) ? { ...field, value: values.find((value) => value.selector === field.selector)?.value || field.value, status: 'filled', source: field.category === 'fixed' ? 'User Profile' : 'Reusable data' } : field)); setToast(`${response.filled} fields filled — review before submitting`); } catch (error) { notifyError(error); } finally { setBusy(''); } };
  const generateField = async (field: FormField) => { if (!session?.jobId) return; setBusy(`field-${field.id}`); try { const job = await repos.jobs.get(session.jobId); if (!job) throw new Error('Job session is no longer linked to a job.'); const resume = job.resumeId ? await repos.resumes.get(job.resumeId) : undefined; const context = buildFieldContext(field, job, resume, profile, knowledge); const provider = getLLMProvider(settings); const answer = await provider.generateFieldAnswer({ fieldLabel: context.field.label, instructions: context.field.instructions, customPrompt: context.field.prompt, job, resume, profile, knowledge: context.knowledge.map((item) => item.answer) }); setFields((items) => items.map((item) => item.id === field.id ? { ...item, value: answer, status: 'review', source: provider.name } : item)); setSession((value) => value ? { ...value, generatedAnswers: [...value.generatedAnswers, { fieldId: field.id, question: field.label, answer, createdAt: now() }], updatedAt: now() } : value); } catch (error) { notifyError(error); } finally { setBusy(''); } };
  const saveApplication = async () => { if (!session?.jobId) return; setBusy('save'); try { const job = await repos.jobs.get(session.jobId); if (!job) throw new Error('Job not found'); const stamp = now(); const finalAnswers = fields.filter((field) => field.value).map((field) => ({ fieldId: field.id, question: field.label, answer: field.value, createdAt: stamp, edited: field.source === 'User edit' })); const updated = { ...job, status: 'Applied' as const, statusHistory: [...(job.statusHistory || []), { timestamp: stamp, previousStatus: job.status, newStatus: 'Applied' as const }], lastActivityAt: stamp }; await repos.jobs.put(updated); sync('job', job.id, updated); const closedSession = { ...session, status: 'closed' as const, updatedAt: stamp, generatedAnswers: finalAnswers, events: [...session.events, { id: uid('event'), type: 'application_submitted', timestamp: stamp }] }; await repos.sessions.put(closedSession); sync('session', session.id, closedSession); const application = { id: uid('application'), jobId: job.id, sessionId: session.id, resumeId: job.resumeId, resumeVersionId: job.resumeVersionId, submittedAt: stamp, answers: finalAnswers, metadata: { pageUrl: session.url, fieldsReviewed: fields.length } }; await repos.applications.put(application); sync('application', application.id, application); setSession(null); await refresh(); setView('dashboard'); setToast('Application saved and marked Applied'); } catch (error) { notifyError(error); } finally { setBusy(''); } };
  const updateJobStatus = async (job: Job, status: Job['status']) => { const updated = { ...job, status, statusHistory: [...(job.statusHistory || []), { timestamp: now(), previousStatus: job.status, newStatus: status }], lastActivityAt: now() }; await repos.jobs.put(updated); sync('job', job.id, updated); setJobs((items) => items.map((item) => item.id === job.id ? updated : item)); };
  const nav = [{ id: 'dashboard' as const, label: 'Dashboard', icon: BriefcaseBusiness }, { id: 'resumes' as const, label: 'Resumes', icon: FileText }, { id: 'session' as const, label: 'Job session', icon: WandSparkles }, { id: 'knowledge' as const, label: 'Knowledge', icon: Sparkles }, { id: 'settings' as const, label: 'Settings', icon: SettingsIcon }];
  return <div className="app-shell"><aside className="sidebar"><div className="brand"><div className="brand-mark"><Sparkles size={17} /></div><div><strong>orbit</strong><span>job assistant</span></div></div><button className="analyze-button" onClick={analyze} disabled={!!busy}><Sparkles size={16} /> {busy === 'analyze' ? 'Analyzing…' : 'Analyze this job'}</button><nav>{nav.map(({ id, label, icon: Icon }) => <button key={id} className={view === id ? 'nav-item active' : 'nav-item'} onClick={() => setView(id)}><Icon size={17} />{label}{id === 'session' && session ? <span className="nav-dot" /> : null}</button>)}</nav><div className="privacy-note"><LockKeyhole size={15} /><span>Local-first by default<br /><small>LLM calls are explicit</small></span></div></aside><main className="main"><header className="topbar"><div><span className="eyebrow">PERSONAL AI WORKSPACE</span><h1>{view === 'dashboard' ? 'Your application pipeline' : nav.find((item) => item.id === view)?.label}</h1></div><div className="top-actions"><button className="icon-button" title="Help"><CircleHelp size={18} /></button><div className="avatar">{profile.fullName?.slice(0, 1).toUpperCase() || 'Y'}</div></div></header>{toast && <div className="toast"><Check size={16} />{toast}<button onClick={() => setToast('')}><X size={14} /></button></div>}<div className="content">{view === 'dashboard' && <Dashboard jobs={jobs} query={query} setQuery={setQuery} onStatus={updateJobStatus} onOpen={(job) => { setSelectedJobId(job.id); if (session?.jobId !== job.id) setSession(null); setView('session'); }} />}{view === 'resumes' && <Resumes resumes={resumes} busy={busy} onUpload={uploadResume} onDelete={async (id) => { await repos.resumes.delete(id); setResumes((items) => items.filter((item) => item.id !== id)); }} />}{view === 'session' && <SessionView session={session} selectedJobId={selectedJobId} jobs={jobs} resumes={resumes} fields={fields} busy={busy} onAnalyze={analyze} onDetect={detectForm} onFill={fillForm} onGenerate={generateField} onChangeField={(id, value) => setFields((items) => items.map((field) => field.id === id ? { ...field, value, status: 'review', source: 'User edit' } : field))} onSave={saveApplication} />}{view === 'settings' && <SettingsView settings={settings} profile={profile} onSettings={async (next) => { setSettings(next); await repos.settings.put(next); setToast('Settings saved'); }} onProfile={async (next) => { setProfile(next); await repos.profile.put(next); setToast('Profile saved'); }} />}{view === 'knowledge' && <KnowledgeView items={knowledge} onAdd={async () => { const item: KnowledgeItem = { id: uid('knowledge'), type: 'custom', question: 'Reusable note', answer: 'Add a reusable fact or preferred answer.', tags: [], source: 'User', confidence: 1, createdAt: now(), updatedAt: now() }; await repos.knowledge.put(item); setKnowledge((items) => [...items, item]); }} onDelete={async (id) => { await repos.knowledge.delete(id); setKnowledge((items) => items.filter((item) => item.id !== id)); }} />}</div></main></div>;
}

function Dashboard({ jobs, query, setQuery, onStatus, onOpen }: { jobs: Job[]; query: string; setQuery: (v: string) => void; onStatus: (job: Job, status: Job['status']) => void; onOpen: (job: Job) => void }) { const filtered = jobs.filter((job) => `${job.title} ${job.company} ${job.location}`.toLowerCase().includes(query.toLowerCase())); const counts = ['Applied', 'Interview', 'Saved'].map((status) => ({ status, count: jobs.filter((job) => job.status === status).length })); return <><div className="hero-row"><div><p className="muted">A clear view of every opportunity, from first look to offer.</p></div><button className="secondary-button" onClick={() => setQuery('')}><RefreshCw size={15} /> Refresh view</button></div><div className="stats">{counts.map((item) => <div className="stat-card" key={item.status}><span className={`status-dot ${statusColors[item.status]}`} /><div><strong>{item.count}</strong><span>{item.status}</span></div><ChevronRight size={17} /></div>)}<div className="stat-card insight"><Activity size={18} /><div><strong>{jobs.length ? `${Math.round(jobs.reduce((sum, job) => sum + (job.matchScore || 0), 0) / jobs.length)}%` : '—'}</strong><span>Average match</span></div></div></div><div className="section-heading"><div><h2>All opportunities</h2><span className="count-pill">{filtered.length}</span></div><label className="search"><Search size={16} /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search company, role, location" /></label></div><div className="job-list">{filtered.length ? filtered.map((job) => <article className="job-card" key={job.id} onClick={() => onOpen(job)}><div className="company-logo">{job.company.slice(0, 1).toUpperCase()}</div><div className="job-main"><div className="job-title-row"><h3>{job.title}</h3><span className={`status-badge ${statusColors[job.status]}`}>{job.status}</span></div><p>{job.company} <span>·</span> {job.location || 'Location not listed'}</p><div className="job-meta"><span>{job.remoteType}</span><span>{job.source}</span><span>Added {new Date(job.discoveredAt).toLocaleDateString()}</span></div></div><div className="match"><strong>{job.matchScore ?? '—'}{job.matchScore ? '%' : ''}</strong><span>match</span></div><select value={job.status} onClick={(event) => event.stopPropagation()} onChange={(event) => onStatus(job, event.target.value as Job['status'])}>{['Saved', 'Analyzing', 'Applied', 'Interview', 'Rejected', 'Offer', 'Withdrawn', 'Archived'].map((status) => <option key={status}>{status}</option>)}</select><ChevronRight className="card-arrow" size={18} /></article>) : <EmptyState icon={<BriefcaseBusiness size={23} />} title="Your pipeline is empty" text="Open a job page and click Analyze this job to create your first session." />}</div></>; }

function Resumes({ resumes, busy, onUpload, onDelete }: { resumes: Resume[]; busy: string; onUpload: (file: File) => void; onDelete: (id: string) => void }) {
  const [dragActive, setDragActive] = useState(false);
  const handleDrop = (event: React.DragEvent<HTMLDivElement>) => {
    event.preventDefault();
    setDragActive(false);
    const file = Array.from(event.dataTransfer.files).find((item) => /\.(pdf|docx)$/i.test(item.name));
    if (file) onUpload(file);
  };
  return <>
    <div className={`resume-dropzone${dragActive ? ' drag-active' : ''}`} onDragEnter={(event) => { event.preventDefault(); setDragActive(true); }} onDragOver={(event) => event.preventDefault()} onDragLeave={(event) => { if (!event.currentTarget.contains(event.relatedTarget as Node)) setDragActive(false); }} onDrop={handleDrop}>
      <div className="hero-row"><div><p className="muted">Keep original documents safe. Adaptations are stored as separate versions.</p><p className="drop-hint">Drop a PDF or DOCX here, or choose a file</p></div><label className="primary-button"><Upload size={16} /> {busy === 'upload' ? 'Parsing…' : 'Upload resume'}<input hidden type="file" accept=".pdf,.docx,application/pdf,application/vnd.openxmlformats-officedocument.wordprocessingml.document" onChange={(event) => { const file = event.target.files?.[0]; event.currentTarget.value = ''; if (file) onUpload(file); }} /></label></div>
    </div>
    <div className="resume-grid">{resumes.map((resume) => <article className="resume-card" key={resume.id}><div className="resume-icon"><FileText size={21} /></div><div className="resume-card-body"><div className="job-title-row"><h3>{resume.name}</h3>{resume.preferred && <span className="preferred">Preferred</span>}</div><p>{resume.fileName} · v{resume.version}</p><div className="tag-row">{resume.targetRoles.slice(0, 3).map((role) => <span key={role}>{role}</span>)}{resume.structuredData.skills.slice(0, 3).map((skill) => <span key={skill}>{skill}</span>)}</div><small>Parsed {resume.parsedText.length.toLocaleString()} characters · {new Date(resume.updatedAt).toLocaleDateString()}</small></div><button className="icon-button danger" title="Delete resume" onClick={() => onDelete(resume.id)}><Trash2 size={16} /></button></article>)}{!resumes.length && <EmptyState icon={<FileText size={23} />} title="Add your first resume" text="PDF and DOCX are parsed locally, then reusable profile data is extracted." />}</div>
  </>;
}

function SessionView({ session, selectedJobId, jobs, resumes, fields, busy, onAnalyze, onDetect, onFill, onGenerate, onChangeField, onSave }: { session: JobSession | null; selectedJobId?: string; jobs: Job[]; resumes: Resume[]; fields: FormField[]; busy: string; onAnalyze: () => void; onDetect: () => void; onFill: () => void; onGenerate: (field: FormField) => void; onChangeField: (id: string, value: string) => void; onSave: () => void }) { const job = jobs.find((item) => item.id === (session?.jobId || selectedJobId)); const best = job?.matches?.[0]; if (!job) return <EmptyState icon={<WandSparkles size={23} />} title="No active job session" text="Open a vacancy in the active tab, then use Analyze this job." action={<button className="primary-button" onClick={onAnalyze}><Sparkles size={16} /> Analyze current tab</button>} />; return <><div className="session-banner"><div className="session-icon"><Sparkles size={20} /></div><div><span className="eyebrow">{session ? 'ACTIVE SESSION' : 'JOB DETAILS'}</span><h2>{job.title}</h2><p>{job.company} · {job.location || 'Location not listed'}</p></div><span className="live-pill"><span /> {session ? 'Live' : job.status}</span></div><div className="session-grid"><section className="panel analysis-panel"><div className="panel-heading"><div><span className="eyebrow">JOB ANALYSIS</span><h2>{job.analysis?.summary || 'Analysis pending'}</h2></div><span className="provider-chip"><Sparkles size={13} /> {job.analysis ? 'Ready' : 'Pending'}</span></div>{job.analysis ? <><div className="analysis-score"><div className="score-ring"><strong>{best?.score ?? '—'}</strong><span>/100</span></div><div><strong>{best?.recommendation || 'Review available resumes'}</strong><p>Explainable score based on requirements, skills and relevant experience.</p></div></div><div className="analysis-columns"><div><label>Must-have requirements</label>{job.analysis.mandatoryRequirements.slice(0, 6).map((item) => <span className="check-line" key={item}><Check size={14} />{item}</span>)}</div><div><label>Keywords</label><div className="tag-row">{job.analysis.keywords.slice(0, 10).map((item) => <span key={item}>{item}</span>)}</div></div></div></> : <button className="primary-button" onClick={onAnalyze} disabled={!!busy}><Sparkles size={15} /> Run analysis</button>}</section><section className="panel resume-match-panel"><div className="panel-heading"><div><span className="eyebrow">RESUME POOL</span><h2>Best matches</h2></div><span className="count-pill">{job.matches?.length || 0}</span></div>{job.matches?.length ? job.matches.map((match) => { const resume = resumes.find((item) => item.id === match.resumeId); return <div className="match-row" key={match.resumeId}><div className="mini-file"><FileText size={15} /></div><div><strong>{resume?.name || 'Resume'}</strong><p>{match.matchingSkills.slice(0, 3).join(' · ') || 'Review fit details'}</p></div><div className="mini-score">{match.score}%</div></div>; }) : <p className="muted">Upload a resume to compare it with this role.</p>}</section></div><section className="panel assistant-panel"><div className="panel-heading"><div><span className="eyebrow">APPLICATION ASSISTANT</span><h2>Detect and fill the current form</h2><p className="muted">The assistant only reads and fills fields. You remain in control of Submit.</p></div><div className="button-row"><button className="secondary-button" onClick={onDetect} disabled={busy === 'detect'}><Search size={15} /> {busy === 'detect' ? 'Detecting…' : 'Detect fields'}</button>{fields.length > 0 && <button className="primary-button" onClick={onFill} disabled={busy === 'fill'}><WandSparkles size={15} /> Fill preview</button>}</div></div>{fields.length > 0 && <><div className="field-summary"><strong>{fields.length} fields detected</strong><span>{fields.filter((field) => field.category !== 'ignore').length} can be assisted</span><span>{fields.filter((field) => field.confidence < 0.8).length} require review</span></div><div className="fields-table">{fields.map((field) => <div className="field-row" key={field.id}><div className="field-kind"><span className={`kind-icon ${field.category}`}><IconFor category={field.category} /></span><span>{field.category}</span></div><div className="field-label"><strong>{field.label}</strong><small>{field.name || field.type} · {Math.round(field.confidence * 100)}% confidence</small></div><input value={field.value} onChange={(event) => onChangeField(field.id, event.target.value)} placeholder={field.category === 'llm' ? 'Generate an answer or type your own' : 'No value available'} /><button className="small-button" onClick={() => onGenerate(field)} disabled={busy === `field-${field.id}`}>{busy === `field-${field.id}` ? '…' : field.category === 'llm' ? 'Generate' : 'Suggest'}</button></div>)}</div><div className="submit-bar">{session ? <><div><strong>Ready to save?</strong><span>Review every answer and submit on the site yourself.</span></div><button className="primary-button" onClick={onSave} disabled={busy === 'save'}><Send size={15} /> Application submitted</button></> : <span className="muted">Start a session to link this form to an application.</span>}</div></>}</section></>; }

const IconFor = ({ category }: { category: FormField['category'] }) => category === 'fixed' ? <LockKeyhole size={13} /> : category === 'llm' ? <Sparkles size={13} /> : category === 'reusable' ? <RefreshCw size={13} /> : <X size={13} />;
function SettingsView({ settings, profile, onSettings, onProfile }: { settings: Settings; profile: UserProfile; onSettings: (value: Settings) => void; onProfile: (value: UserProfile) => void }) { const [tab, setTab] = useState<'profile' | 'llm' | 'parser'>('profile'); const [local, setLocal] = useState(settings); const [person, setPerson] = useState(profile); const [models, setModels] = useState<OpenRouterModel[]>([]); const patch = (key: keyof Settings, value: unknown) => setLocal({ ...local, [key]: value }); const patchPerson = (key: keyof UserProfile, value: string) => setPerson({ ...person, [key]: value }); const loadModels = async () => { try { setModels(await fetchOpenRouterModels(local.openRouterApiKey)); } catch { /* the editable model field and local fallback stay available */ } }; return <><div className="settings-tabs">{[['profile', 'User profile', UserRound], ['llm', 'LLM provider', Sparkles], ['parser', 'Job parser', Activity]].map(([id, label, Icon]) => <button key={id as string} className={tab === id ? 'active' : ''} onClick={() => setTab(id as typeof tab)}>{React.createElement(Icon as typeof UserRound, { size: 16 })}{label as string}</button>)}</div>{tab === 'profile' && <div className="panel settings-panel"><div className="panel-heading"><div><span className="eyebrow">PERSONAL DATA</span><h2>User profile</h2><p className="muted">Fixed values are used for high-confidence autofill.</p></div><button className="primary-button" onClick={() => onProfile(person)}>Save profile</button></div><div className="form-grid">{(['firstName', 'lastName', 'fullName', 'email', 'phone', 'address', 'city', 'province', 'postalCode', 'linkedin', 'portfolio', 'website', 'workAuthorization'] as const).map((key) => <label key={key}><span>{key.replace(/[A-Z]/g, (m) => ` ${m}`).replace(/^./, (m) => m.toUpperCase())}</span><input value={person[key]} onChange={(event) => patchPerson(key, event.target.value)} /></label>)}</div><label className="wide-field"><span>Skills (comma separated)</span><input value={person.skills.join(', ')} onChange={(event) => setPerson({ ...person, skills: event.target.value.split(',').map((v) => v.trim()).filter(Boolean) })} /></label></div>}{tab === 'llm' && <div className="panel settings-panel"><div className="panel-heading"><div><span className="eyebrow">PROVIDER</span><h2>LLM settings</h2><p className="muted">Keys stay in this browser's IndexedDB and are sent only to the selected provider.</p></div><button className="primary-button" onClick={() => onSettings(local)}>Save settings</button></div><div className="form-grid"><label><span>Provider</span><select value={local.llmProvider}><option>OpenRouter</option></select></label><label><span>OpenRouter model</span><div className="input-action"><input list="openrouter-models" value={local.openRouterModel} onChange={(event) => patch('openRouterModel', event.target.value)} placeholder="e.g. openai/..." /><button className="small-button" type="button" onClick={loadModels}>Load models</button><datalist id="openrouter-models">{models.map((model) => <option key={model.id} value={model.id}>{model.name}</option>)}</datalist></div></label><label className="wide-field"><span>API key</span><input type="password" value={local.openRouterApiKey} onChange={(event) => patch('openRouterApiKey', event.target.value)} placeholder="sk-or-…" /></label><label><span>Temperature</span><input type="number" min="0" max="2" step="0.1" value={local.temperature} onChange={(event) => patch('temperature', Number(event.target.value))} /></label><label><span>Max tokens</span><input type="number" min="100" max="8000" value={local.maxTokens} onChange={(event) => patch('maxTokens', Number(event.target.value))} /></label></div><div className="callout"><Sparkles size={16} /><span>No key? Orbit uses a local heuristic fallback, so your data remains local while you test the workflow.</span></div></div>}{tab === 'parser' && <div className="panel settings-panel"><div className="panel-heading"><div><span className="eyebrow">JOB DISCOVERY</span><h2>Apify parser</h2><p className="muted">Phase 2 provider interface is ready. Configure it for scheduled discovery.</p></div><button className="primary-button" onClick={() => onSettings(local)}>Save settings</button></div><div className="form-grid"><label><span>Enabled</span><select value={String(local.parserEnabled)} onChange={(event) => patch('parserEnabled', event.target.value === 'true')}><option value="false">Off</option><option value="true">On</option></select></label><label><span>Schedule (cron)</span><input value={local.parserSchedule} onChange={(event) => patch('parserSchedule', event.target.value)} /></label><label className="wide-field"><span>Apify API key</span><input type="password" value={local.apifyApiKey} onChange={(event) => patch('apifyApiKey', event.target.value)} /></label><label className="wide-field"><span>Actor ID</span><input value={local.apifyActor} onChange={(event) => patch('apifyActor', event.target.value)} placeholder="username/actor-name" /></label><label><span>Job titles</span><input value={local.jobTitles.join(', ')} onChange={(event) => patch('jobTitles', event.target.value.split(',').map((v) => v.trim()).filter(Boolean))} /></label><label><span>Locations</span><input value={local.locations.join(', ')} onChange={(event) => patch('locations', event.target.value.split(',').map((v) => v.trim()).filter(Boolean))} /></label></div></div>}<ServerSyncPanel settings={local} onChange={(key, value) => patch(key, value)} onSave={() => onSettings(local)} /></>; }
function KnowledgeView({ items, onAdd, onDelete }: { items: KnowledgeItem[]; onAdd: () => void; onDelete: (id: string) => void }) { return <><div className="hero-row"><div><p className="muted">Reusable answers and professional facts you choose to keep between applications.</p></div><button className="primary-button" onClick={onAdd}><Plus size={16} /> Add knowledge</button></div><div className="knowledge-list">{items.map((item) => <article className="knowledge-card" key={item.id}><div className="knowledge-type">{item.type.replace('_', ' ')}</div><div><strong>{item.question || 'Reusable knowledge'}</strong><p>{item.answer}</p><small>{item.tags.join(' · ') || 'No tags'} · {item.source}</small></div><button className="icon-button danger" onClick={() => onDelete(item.id)}><Trash2 size={16} /></button></article>)}{!items.length && <EmptyState icon={<Sparkles size={23} />} title="Your knowledge base is quiet" text="Save preferred answers and reusable facts here as you work through applications." />}</div></>; }
function EmptyState({ icon, title, text, action }: { icon: React.ReactNode; title: string; text: string; action?: React.ReactNode }) { return <div className="empty-state"><div className="empty-icon">{icon}</div><h2>{title}</h2><p>{text}</p>{action}</div>; }

function ServerSyncPanel({ settings, onChange, onSave }: { settings: Settings; onChange: (key: keyof Settings, value: unknown) => void; onSave: () => void }) { const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [busy, setBusy] = useState(false); const [message, setMessage] = useState(''); const login = async () => { setBusy(true); setMessage(''); try { const result = await loginBackend(settings.backendUrl, email, password); onChange('backendToken', result.access_token); onChange('syncEnabled', true); setPassword(''); setMessage('Connected'); } catch (error) { setMessage(error instanceof Error ? error.message : 'Login failed'); } finally { setBusy(false); } }; return <div className="panel settings-panel server-panel"><div className="panel-heading"><div><span className="eyebrow">ORACLE SERVER</span><h2>Cloud sync</h2><p className="muted">Sign in once on each computer. Records are saved locally first and never blocked by a server outage.</p></div><button className="primary-button" onClick={onSave}>Save sync</button></div><div className="form-grid"><label><span>Enabled</span><select value={String(settings.syncEnabled)} onChange={(event) => onChange('syncEnabled', event.target.value === 'true')}><option value="false">Off</option><option value="true">On</option></select></label><label><span>API URL</span><input value={settings.backendUrl} onChange={(event) => onChange('backendUrl', event.target.value)} placeholder="https://api.example.com" /></label><label><span>Account email</span><input type="email" value={email} onChange={(event) => setEmail(event.target.value)} placeholder="you@example.com" /></label><label><span>Password</span><input type="password" value={password} onChange={(event) => setPassword(event.target.value)} placeholder="Server account password" /></label><label className="wide-field"><span>Session token</span><input type="password" value={settings.backendToken} onChange={(event) => onChange('backendToken', event.target.value)} placeholder="Filled after sign in" /></label></div><div className="button-row"><button className="secondary-button" onClick={login} disabled={busy || !settings.backendUrl || !email || !password}>{busy ? 'Signing in…' : 'Sign in to Oracle'}</button>{message && <span className="muted">{message}</span>}</div></div>; }

createRoot(document.getElementById('root')!).render(<App />);
