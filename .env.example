# The MVP stores provider keys locally in the extension Settings page.
# This file is reserved for future build-time defaults; do not commit secrets.
VITE_DEFAULT_OPENROUTER_MODEL=
ORBIT_DB_PASSWORD=replace-with-a-long-database-password
ORBIT_HTTP_PORT=8088
