# Browser-only provider keys are stored in extension Settings. Oracle provider
# keys belong in backend/.env. Never place secrets in VITE_* values.
VITE_DEFAULT_OPENROUTER_MODEL=
ORBIT_DB_PASSWORD=replace-with-a-long-database-password
ORBIT_HTTP_PORT=8088
